openapi: 3.0.0
info:
  title: Customer API
  version: 1.0.0

paths:
  /customers:
    get:
      summary: Get customers
      responses:
        '200':
          description: successful response

    post:
      summary: Create customer
      requestBody:
        required: true
      responses:
        '201':
          description: created